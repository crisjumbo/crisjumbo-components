import { Input } from "../../atoms/Input/Input"

export const Searcher = () => {
    return (
        <Input name="default" placeholder="Search..." type="text" value="" handleChange={() => {}} />
    )
}